---
import { findImage } from '../utils/images';
const appStoreLogo = await findImage('~/assets/images/logo_muguiwara_.png');
---

{appStoreLogo && <img src={appStoreLogo.src} alt="logo Muguiwaras" width="40%" />}
